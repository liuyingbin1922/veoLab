ANTHROPIC_API_KEY=your_claude_key
GOOGLE_CLOUD_PROJECT=yb-gemini-251125
VERTEXAI_LOCATION=us-central1                      # 可选，默认 us-central1
VERTEXAI_API_ENDPOINT=us-central1-aiplatform.googleapis.com  # 可选，自定义 Vertex 入口
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/service-account.json # 服务账号凭证，或使用 ADC
CLAUDE_MODEL=claude-3-5-sonnet-20241022           # 可选
GEMINI_MODEL=gemini-3-pro-preview             # 可选
RATE_LIMIT_PER_IP=100                                # 可选，默认 3